#!/bin/bash

removedExtensions="*.dvi *.aux *.log *.bbl *.blg *.nav *.out *.snm *.toc *.synctex.gz *.fls *.fdb_latexmk"

#O directorio onde se encontra os arquivos pode ser passado como um argumento opcional
[ $# -eq 1 ] && cd $1

rm $removedExtensions  2> /dev/null
rm -rf auto 2> /dev/null

